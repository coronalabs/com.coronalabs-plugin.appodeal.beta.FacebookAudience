Sources for the plugin `plugin.appodeal.beta.FacebookAudience`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.FacebookAudience" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
